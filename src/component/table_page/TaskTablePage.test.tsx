import React from 'react'
import TestRenderer from 'react-test-renderer';
import TaskTableElement from './task-table-element/TaskTableElement';
import TaskTablePage from './TaskTablePage';

// const page = jest.fn().mockImplementation(() => null);

describe('TaskTablePage', () => {
    it('проверка ширины отрисованной компоненты', () => {
        
    })
} )
